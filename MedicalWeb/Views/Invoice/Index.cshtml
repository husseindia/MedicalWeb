@model IEnumerable<MedicalWeb.Models.InvoiceClass>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.pharmName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.medicalName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.quantiti)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.price)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.pharmName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.medicalName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.quantiti)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.price)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
